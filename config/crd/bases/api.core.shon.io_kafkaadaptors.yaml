---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: kafkaadaptors.api.core.shon.io
spec:
  group: api.core.shon.io
  names:
    kind: KafkaAdaptors
    listKind: KafkaAdaptorsList
    plural: kafkaadaptors
    singular: kafkaadaptors
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KafkaAdaptors is the Schema for the kafkaadaptors API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KafkaAdaptorsSpec defines the desired state of KafkaAdaptors.
            properties:
              image:
                type: string
              kafkaConnection:
                description: Foo is an example field of KafkaAdaptors. Edit kafkaadaptors_types.go
                  to remove/update
                properties:
                  bootstrapServer:
                    items:
                      type: string
                    type: array
                  caCert:
                    type: string
                  host:
                    type: string
                  password:
                    type: string
                  username:
                    type: string
                required:
                - bootstrapServer
                - caCert
                - host
                - password
                - username
                type: object
              transformations:
                items:
                  properties:
                    name:
                      type: string
                    sourceTopic:
                      type: string
                    targetTopic:
                      type: string
                    transformationType:
                      type: string
                  required:
                  - name
                  - sourceTopic
                  - targetTopic
                  - transformationType
                  type: object
                type: array
            required:
            - image
            - kafkaConnection
            - transformations
            type: object
          status:
            description: KafkaAdaptorsStatus defines the observed state of KafkaAdaptors.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
